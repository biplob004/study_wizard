# Get a key from https://aistudio.google.com/app/apikey
# Required only for the two LLM-judged exercises (type-answer, fill-blank).
# Without it, those exercises fall back to tolerant string matching.
# Also used by the Gemini image generator helper (scripts/playground/gemini_image.py).
GOOGLE_API_KEY=
# Used by the OpenAI image and audio generators under backend/scripts/.
OPENAI_API_KEY=
SARVAMAI_API_KEY=
# Optional: override the Gemini image-generation model used by gemini_image.py.
# Default is gemini-3.1-flash-image (~$0.067 per 1024x1024 image).
# GEMINI_IMAGE_MODEL=gemini-3.1-flash-image
