"""Developer scripts and experimental features that are not part of the core API.

Contains the image/audio generators (run with ``python -m scripts.<pkg>.generate``) and the
Gemini image-generation playground router mounted by the app.
"""
