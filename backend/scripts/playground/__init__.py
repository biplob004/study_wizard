"""Playground: experimental, self-contained helpers that aren't part of the core API.

Currently holds ``gemini_image``, a Gemini-powered image generator that writes straight into the same
``data/images`` folder the vocabulary endpoints serve from. It's an importable utility only — there is
no HTTP endpoint for it.
"""
