"""Playground: experimental, self-contained features that aren't part of the core API yet.

Currently hosts a Gemini-powered image generator that writes straight into the same
``data/images`` folder the vocabulary endpoints serve from.
"""
